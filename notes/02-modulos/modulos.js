const utils = require('./utils');
const str = 'Probando los módulos de node.js';

console.log('Texto capitalizado ->' + utils.capitalize(str));
console.log('Texto al revés ->' + utils.str_reverse(str));