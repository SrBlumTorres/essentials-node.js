import { capitalize, str_reverse } from './utils.mjs' 

const str = 'sistema de módulos - eCMAScript modules';

console.log('Texto capitalizado -> ' + capitalize(str));
console.log('Texto al revés -> ' + str_reverse(str));

